
//<button onClick={()=> navigate("/search")}>Hello friend!</button>

/*<a href="/search">Hello friend!</a>*/

/*switch(window.location.pathname){
    case "/":
      break;
    case "/login":
      comp = <SignIn />;
      break;
    case "/regester":
      comp = <SignUp  />;
      break;
    case "/search":
      comp = <ListOfTopics  />;
    break;
  }*/


//const [data, setData] = useState([]);
/*const getList = async () => {
        try {
            const response = await fetch("http://localhost:4000/todos");
            const jsonData = await response.json();
            setData(jsonData);


        } catch (error) {
            console.error(error);
        }
    }*/

//let dataArr;
//useEffect(() => {getList();});

    //console.log(data);

    //dataArr = Object.values(data);
/*return (<form>
        {<table>
            <thead>
                <tr>
                    <th>
                        Account
                    </th>
                    <th>
                        Password
                    </th>
                </tr>
            </thead>
            <tbody>
                {dataArr.map(el => (
                    <tr>
                        <td>{el.account_name}</td>
                        <td>{el.account_password}</td>
                    </tr>
                ))}
            </tbody>
        </table>}
        <button onClick={()=> navigate("/account")}>Go to account</button>
    </form>);*/